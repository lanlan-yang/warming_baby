# GUI 框架
PyQt6==6.11.0

# 配置与数据验证
pydantic==2.13.4
pydantic-settings==2.14.2

# 日志
loguru==0.7.3

# LangChain + LangGraph (Agent 核心)
langchain==1.3.11
langchain-core==1.5.2
langchain-openai==1.4.1
langgraph==1.2.7

# Qt + asyncio 集成 (必需，用于 QEventLoop)
qasync==0.28.0

# macOS 系统级 API (用于窗口置顶) - 仅 macOS 需要
pyobjc-framework-Cocoa==12.2.1; platform_system == "Darwin"

# PyTorch - 深度学习框架 (Embedding 模型推理)
# Windows (NVIDIA GPU): 需用 CUDA 12.8 index 安装 (支持 RTX 50系 Blackwell):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0; platform_system == "Windows"
# macOS: PyPI 默认安装 (MPS 加速)
torch>=2.7.0; platform_system == "Darwin"

# 向量数据库 (记忆系统核心)
chromadb==1.5.9

# Embedding 模型加载 (用于记忆系统)
sentence-transformers==5.6.0

# MCP (Model Context Protocol) - 外部工具标准协议
mcp==2.0.0
