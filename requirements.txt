PyQt6>=6.11.0
pydantic>=2.13.0
pydantic-settings>=2.12.0
loguru>=0.7.0

# LangChain + LangGraph (Agent 核心)
langchain>=1.3.0
langchain-core>=1.4.0
langchain-openai>=1.3.0
langgraph>=0.2.0

# 注意: v0.1 版本移除了 fastapi/uvicorn
# 如果未来需要 HTTP API，可以再添加回去
